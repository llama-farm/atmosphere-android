package com.llamafarm.atmosphere.transport

/**
 * LEGACY type definitions for backwards compatibility.
 * BLE transport has been removed - will be re-added as Rust transport in atmosphere-core.
 * These stubs are kept to maintain compilation while UI is being updated.
 */

// Node information (BLE peer discovery)
typealias NodeInfo = com.llamafarm.atmosphere.network.NodeInfo
